New TYPE,

`any` allows to pass any type.

```
fn foo(x: any)
{
    T :: @type(x)
    when T {
        i64 => ...,
        i32 => ...,
        str =>...,
        MyStruct => ...,
        ...
        _ => ...,
    }
```
